# 会话记录 - 2025-08-10



> 2. 分析 pi agent 机制并为 myagent 项目提供功能建议

---



## 第二部分：pi agent 机制详解

### 1. Steering 机制

pi agent 支持在工作时排队提交消息来实时干预 agent 行为。

| 快捷键 | 类型 | 行为 |
|--------|------|------|
| **Enter** | Steering 消息 | 当前回合执行完工具调用后立即投递 |
| **Alt+Enter** | Follow-up 消息 | agent 完成所有工作后才投递 |
| **Escape** | 取消 | 中断并恢复排队消息到编辑器 |
| **Alt+↑** | 撤回 | 取回排队消息到编辑器 |

**配置选项：**
```json
{
  "steeringMode": "one-at-a-time",  // 或 "all"
  "followUpMode": "one-at-a-time"
}
```

### 2. 会话树形结构 (Session Tree)

```
用户消息 A → 助手回复 A1
           ├─ 选择 B → 助手回复 B1 → 选择 C...
           └─ 回到 A → 选择 D → 助手回复 D1...
```

**特点：**
- 所有分支保存在同一个 JSONL 文件中
- 每个条目有 `id` 和 `parentId`
- 可以随时切换分支，所有历史完整保留

**命令：**
- `/tree` - 在原会话中浏览/跳转
- `/fork` - 从历史消息创建新会话文件
- `/clone` - 复制当前分支到新会话

### 3. 上下文压缩

**触发条件：**
- 自动：`contextTokens > contextWindow - reserveTokens`（默认预留 16K tokens）
- 手动：`/compact [instructions]`

**工作原理：**
```
[旧消息 × N] → [压缩条目] [保留的最近消息]
                ↑ 调用 LLM 总结
```

**配置：**
```json
{
  "keepRecentTokens": 20000,
  "reserveTokens": 16384
}
```

### 4. 扩展系统

**核心能力：**

| 功能 | API |
|------|-----|
| 自定义工具 | `pi.registerTool()` |
| 命令注册 | `pi.registerCommand()` |
| 事件拦截 | `pi.on("tool_call", ...)` |
| 用户交互 | `ctx.ui.confirm()`, `ctx.ui.select()` |
| 自定义 UI | `ctx.ui.custom()` |
| 状态持久化 | `pi.appendEntry()` |

**可用事件（30+）：**
- 生命周期：`session_start`, `session_end`
- 资源：`project_trust`, `context_files_loaded`
- 会话：`branch_switched`, `compacted`
- Agent：`user_prompt`, `assistant_response`
- 模型：`model_changed`
- 工具：`tool_call`, `tool_result`

**示例用例：**
- 权限门禁（阻止 `rm -rf`）
- Git 检查点（每回合 stash）
- 自定义压缩
- 交互式工具
- 外部集成

### 5. Pi 包管理

```bash
pi install npm:@foo/bar@1.0.0
pi install git:github.com/user/repo@v1
pi list
pi update --all
```

**可包含内容：**
- Extensions（扩展）
- Skills（技能）
- Prompt Templates（提示模板）
- Themes（主题）

### 6. 技能机制

遵循 [Agent Skills 标准](https://agentskills.io)：

```markdown
# My Skill
Use this skill when the user asks about X.

## Steps
1. Do this
2. Then that
```

**加载位置：**
- `~/.pi/agent/skills/`
- `.pi/skills/`
- Pi 包中

**调用方式：**
- 自动：Agent 根据任务需求加载
- 手动：`/skill:name`

### 7. 思考级别控制

**级别：** `off`, `minimal`, `low`, `medium`, `high`, `xhigh`, `max`

**切换方式：**
- `Shift+Tab` - 循环切换
- `/settings` - UI 配置
- `--thinking <level>` - CLI 参数
- 模型简写：`--model sonnet:high`

### 8. 模型轮换

**配置：**
```json
{
  "models": ["claude-sonnet-4*", "gpt-4o", "gemini-2.5*"]
}
```

**快捷键：**
- `Ctrl+L` - 打开模型选择器
- `Ctrl+P` / `Shift+Ctrl+P` - 向前/向后轮换

### 9. 上下文文件

自动加载的指导文件：

| 文件 | 作用 |
|------|------|
| `AGENTS.md` | 项目指令 |
| `CLAUDE.md` | 同上（兼容） |
| `AGENTS.override.md` | 覆盖同级文件 |
| `.pi/SYSTEM.md` | 替换系统提示 |
| `APPEND_SYSTEM.md` | 追加到系统提示 |

### 10. 项目信任机制

**问题：** 项目可能包含危险扩展或设置

**解决方案：**
- 启动时询问是否信任项目
- 未信任前只加载全局资源
- 信任后才加载项目本地扩展/设置

**配置：**
```json
{
  "defaultProjectTrust": "ask" | "always" | "never"
}
```

### 11. 其他机制

- **多模态支持：** 图片粘贴、拖拽、文件引用
- **会话持久化：** 自动保存到 JSONL，支持命名、导出
- **传输协议选择：** SSE / WebSocket / Auto
- **SDK 模式：** 嵌入到 Node.js 应用
- **RPC 模式：** stdin/stdout JSONL 协议
- **主题系统：** 热重载支持

---

## 第三部分：myagent 项目功能建议

### 优先级 1：核心体验增强

#### 1. 会话树形分支可视化 ⭐⭐⭐⭐⭐
**当前状态：** 只显示线性会话列表

**建议实现：**
- 侧边栏会话列表支持树形视图
- 显示分支关系（缩进+连接线）
- 支持在树中切换/分支/合并
- `/tree` 命令对应的前端可视化

**收益：** 充分利用 Pi 的会话树特性，探索不同解决方案

---

#### 2. Steering 消息队列 UI ⭐⭐⭐⭐⭐
**当前状态：** InputBar 只有"发送"按钮

**建议实现：**
```
┌─────────────────────────────────────┐
│  [输入框]              [发送] [队列] │
└─────────────────────────────────────┘
  点击"队列" → 选择类型
  ├── 🎯 Steering (Enter) - 当前回合后投递
  └── ⏳ Follow-up (Alt+Enter) - 全部完成后投递

  显示已排队消息（可编辑/删除）
```

**收益：** 实时干预 Agent 工作流，真正发挥 Pi 的 Steering 机制

---

#### 3. 思考级别控制 ⭐⭐⭐⭐
**当前状态：** 没有可见的 UI 控制

**建议实现：**
- 状态栏添加思考级别指示器：`🧠 Medium`
- 点击切换：`Off → Minimal → Low → Medium → High → XHigh → Max`
- 简捷键 `Shift+Tab`
- 模型选择时支持 `sonnet:high` 语法

**收益：** 根据任务复杂度动态调整，节省成本/提升质量

---

#### 4. Compaction 可视化 ⭐⭐⭐⭐
**当前状态：** 用户看不到压缩发生了什么

**建议实现：**
- 消息列表中显示压缩条目标记：`📦 上下文已压缩 (节省 12K tokens)`
- 点击可查看压缩摘要内容
- 手动触发 `/compact` 按钮
- 显示当前上下文使用量进度条

**收益：** 上下文透明度，避免"为什么 Agent 忘记了之前的内容"

---

### 优先级 2：工作流增强

#### 5. Agent 技能市场 ⭐⭐⭐⭐
**当前状态：** 有 Agent 管理，但没有技能发现机制

**建议实现：**
- 内置技能库（参考 `~/.agents/skills/`）
- 技能商店 UI（浏览/搜索/安装）
- 支持从 npm/git 安装 Pi 包
- 技能预览（显示 SKILL.md 内容）
- 项目级 vs 全局级技能管理

**收益：** 社区生态，技能复用

---

#### 6. Agent 扩展市场 ⭐⭐⭐⭐
**当前状态：** 有子 Agent 和 Todo 扩展，但没有管理 UI

**建议实现：**
- 扩展管理面板（启用/禁用/配置）
- 从 npm/git 安装扩展
- 扩展事件可视化（开发调试）
- 扩展权限审计（显示扩展的 API 访问）

**收益：** 扩展生态，安全性透明

---

#### 7. Git 集成增强 ⭐⭐⭐⭐
**当前状态：** 有分支选择，但功能有限

**建议实现：**
- 提交历史时间线（与消息对齐）
- Diff 预览（显示 Agent 修改的内容）
- 交互式 rebase/merge 支持
- 自动提交每轮对话的更改（可选）
- Git blame 集成到代码预览

**收益：** 与开发工作流深度集成

---

#### 8. 快速提示词管理器增强 ⭐⭐⭐
**当前状态：** 有 QuickPromptManager，但功能可扩展

**建议实现：**
- 提示词变量（如 `{{project}}`, `{{file}}`）
- 提示词分类/标签
- 提示词搜索
- 提示词分享/导入导出
- AI 生成提示词建议

**收益：** 提升重复任务效率

---

### 优先级 3：用户体验

#### 9. 主题系统 ⭐⭐⭐⭐
**当前状态：** 已实现 `useThemeStore`，但用户无法自定义

**建议实现：**
- 主题编辑器（颜色、字体、间距）
- 社区主题分享
- 主题市场
- 跟随系统主题

**收益：** 个性化体验

---

#### 10. 键盘快捷键配置 ⭐⭐⭐
**当前状态：** Pi 支持自定义快捷键，但 Web 端未暴露

**建议实现：**
- 快捷键配置面板
- 快捷键冲突检测
- 快捷键提示（类似 VS Code 的 `Cmd+K` 菜单）
- 可导出快捷键配置

**收益：** 专业用户效率

---

#### 11. 多窗口支持 ⭐⭐⭐
**当前状态：** 单窗口，无法同时查看多个会话

**建议实现：**
- 新窗口打开会话（`Cmd+Shift+N`）
- 窗口间拖拽消息/代码
- 窗口同步（协作场景）

**收益：** 并行工作

---

#### 12. 通知系统 ⭐⭐⭐
**当前状态：** Agent 完成任务时没有明显通知

**建议实现：**
- 浏览器通知权限
- 任务完成/失败通知
- 警告/错误通知
- 通知历史

**收益：** 不用一直盯着屏幕

---

### 优先级 4：数据洞察

#### 13. 使用统计仪表盘 ⭐⭐⭐
**当前状态：** 状态栏显示 token/cost，但缺少历史分析

**建议实现：**
- Token 使用趋势图（按天/周）
- Cost 统计（按模型/Agent）
- 响应时间分析
- 成功失败率
- Agent/Team 使用频率统计

**收益：** 成本控制，性能优化

---

#### 14. 会话搜索 ⭐⭐⭐
**当前状态：** 历史会话只能按时间浏览

**建议实现：**
- 全局搜索（跨所有会话）
- 搜索消息/代码/文件引用
- 搜索结果高亮
- 保存常用搜索

**收益：** 快速找到之前的工作

---

#### 15. Agent 性能分析 ⭐⭐⭐
**当前状态：** 有 Agent 执行流程图，但缺少性能数据

**建议实现：**
- 每个 Agent 的平均响应时间
- Token 消耗对比
- 成功失败率
- 工具调用频率
- 性能排行榜

**收益：** 优化 Agent 配置

---

### 优先级 5：安全与协作

#### 16. 项目信任管理 UI ⭐⭐⭐⭐
**当前状态：** Pi 有项目信任机制，但 Web 端未暴露

**建议实现：**
- 信任项目列表
- 信任详情（哪些扩展/技能已加载）
- 撤销信任
- 信任策略配置（`ask|always|never`）

**收益：** 安全性透明

---

#### 17. 协作功能 ⭐⭐
**当前状态：** 单机使用

**建议实现：**
- 会话分享（只读链接）
- 实时协作编辑
- 评论/标注
- 权限管理

**收益：** 团队协作

---

#### 18. 数据导出 ⭐⭐⭐
**当前状态：** 有 `/export`，但 UI 未暴露

**建议实现：**
- 导出为 Markdown/JSON/PDF
- 导出消息/代码/会话
- 批量导出
- 自定义导出模板

**收益：** 文档/归档

---

### 优先级 6：高级功能

#### 19. 定时任务/Cron 集成 ⭐⭐⭐⭐
**当前状态：** Pi 有 `cron-task` 技能，但 Web 端未集成

**建议实现：**
- 定时任务管理器（创建/编辑/删除）
- Cron 表达式编辑器
- 任务执行历史
- 任务通知

**收益：** 自动化重复任务

---

#### 20. Autopilot 增强 ⭐⭐⭐⭐
**当前状态：** 有 Autopilot，但功能可扩展

**建议实现：**
- Autopilot 任务编辑器
- 任务依赖管理
- 批量执行
- 任务模板
- 自动重试策略

**收益：** 自动化复杂任务

---

#### 21. Agent 对话历史 ⭐⭐⭐
**当前状态：** 子 Agent 的对话历史未可视化

**建议实现：**
- 子 Agent 的完整对话记录
- 消息传递追踪
- 上下文传递可视化

**收益：** 调试复杂 Agent 流程

---

#### 22. 模型 A/B 测试 ⭐⭐
**当前状态：** 未实现

**建议实现：**
- 并行发送给多个模型
- 结果对比视图
- 自动评估

**收益：** 模型选择决策

---

## 实施优先级建议

### 立即实施（高价值/低复杂度）：
1. ✅ **Steering 消息队列 UI** - 充分利用 Pi 核心特性
2. ✅ **思考级别控制** - 简单实现，大收益
3. ✅ **Compaction 可视化** - 提升透明度
4. ✅ **项目信任管理 UI** - 安全必需

### 短期规划（1-2 个月）：
5. ✅ **会话树形分支可视化** - Pi 特色
6. ✅ **Agent 技能市场** - 生态建设
7. ✅ **Git 集成增强** - 开发体验
8. ✅ **使用统计仪表盘** - 数据驱动

### 中期规划（3-6 个月）：
9. ✅ **Agent 扩展市场**
10. ✅ **主题系统**
11. ✅ **会话搜索**
12. ✅ **定时任务集成**

### 长期愿景：
13. ✅ **协作功能**
14. ✅ **多窗口支持**
15. ✅ **模型 A/B 测试**

---

## 项目文件位置

### HyperFrames 视频项目
```
/Users/lixinwei/Documents/IdeaProjects/mall-test/agent-plugins-video/
├── DESIGN.md
├── SCRIPT.md
├── index.html
├── agent-plugins-draft.mp4
└── agent-plugins-final.mp4
```

### myagent 项目结构
```
/Users/lixinwei/myagent/
├── packages/
│   ├── server/          # 后端
│   ├── web/             # 前端
│   ├── pi-orchestrator/
│   ├── pi-subagent-extension/
│   └── pi-todo-extension/
├── docs/                # 文档
└── design/              # 设计文档
```

---

## 技术栈

**后端：** Fastify, WebSocket, SSE
**前端：** React, Zustand, TypeScript
**Agent 框架：** Pi Agent (@earendil-works/pi-coding-agent)
**包管理：** pnpm

---

*会话记录生成时间：2025-08-10*